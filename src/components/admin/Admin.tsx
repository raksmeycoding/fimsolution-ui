import React from 'react';

const Admin = () => {
    return (
        <div className="h-screen flex items-center justify-center w-full text-3xl">
            Welcome to the admin page!
        </div>
    );
};

export default Admin;